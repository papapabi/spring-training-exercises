/**
 * This is new in JPA 2.1.
 * <p>
 * The {@link javax.persistence.AttributeConverter} only supports mapping to a
 * single database column. Future versions of JPA may be able to support
 * multiple columns.
 */
package mapping.converter;